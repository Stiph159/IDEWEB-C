let n = Number(prompt("ingrese el numero a multiplicar"));
let resultado = 0;

for(let i = 0; i <= 12 ; i++ ){

    resultado = i * n;
    console.log("la multiplicacion de: " + i + " * "+ n + "=" + resultado);
}

